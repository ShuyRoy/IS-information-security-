双轨密码是将明文进行奇数位和偶数位分离。
对明文的加密：
①将明文输入，并测量明文的字符个数（使用strlen()函数）；
②判断明文内容的字符个数为偶数个还是奇数个；
③分别把奇数位和偶数位的字符放在两个数组中；
④输出存放奇数位和偶数位的数组。
对密文解密：
①将密文输入，并测量密文的字符个数（使用strlen()函数）；
②判断明文内容的字符个数为偶数个还是奇数个；
③分别将密文的前一半的字符放在存放明文数组的奇数位，后一半放在存放明文数组的偶数位；
④输出明文数组。

for循环都是从0开始，所以逻辑位数和物理位数刚好岔开。