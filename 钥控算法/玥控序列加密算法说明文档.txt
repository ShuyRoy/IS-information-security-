玥控序列加密方法是将明文按照密钥排列成二维数组后，然后将密钥按照其ASCII码大小排列，并将其之前所对应的列依次输出。

①输入密钥；
②对密钥进行字符个数统计，得出二维数组的列；
③对密钥进行从小到大排序（此代码用了快速排序的方法）；

④输入明文；
⑤对明文进行字符个数的统计，不包括空格；
⑥有明文字符个数和密钥个数即可算出二维数组所需的行数（二维数组不将密钥算在内）；

⑦输出密文：
将排序前的密钥与排序后的密钥从第一个开始比较，一直比较到密钥相同，则输出该列；每一列在输出后会有换行。